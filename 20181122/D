/*for loop，計算費式 */
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int a=1,b=1,c,n; 
	printf("請輸入數字：");
    scanf("%d", &n);
	for(c=0; c<=n; c++) 
	{ 
		c=a+b;   
		printf("%d", c); 
		return c;
	} 
}
